你是一个最终结构整理器。现在是你输出最终可交付成果的时候。
请读取 `故事结构_草稿.json`、全书分析报告以及最新的校验报错日志 `validate_report.txt`。

你的任务是：
1. 修正所有校验报告中指出的引用错误（Reference Error）。
2. 补齐所有缺少必填字段（如介绍、标签、首次/最近章节等）的元素。
3. 把未确定的/模糊的占位符清理掉。
4. 将最终完善的完整故事结构按照规范 Schema 输出。

请输出完整的、合规的 JSON。不要带有 Markdown 语法。
