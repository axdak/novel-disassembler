# 第{{ "%03d" | format(chapter_seq) }}章Delta提取任务

## 唯一产出

本章Delta JSON：`{{ delta_path }}`

本任务只做结构提取。**不得修改章节分析MD**；必须基于下方已完成的章节分析、原文和当前故事结构索引输出本章Delta，不要输出完整故事结构JSON。

## 当前故事结构索引

```text
{{ summary }}
```

## 已完成章节分析

```markdown
{{ analysis_text }}
```

## Delta JSON 格式与字段示范

**结构骨架**（顶层键）：

```json
{
  "章节": "第{{ "%03d" | format(chapter_seq) }}章",
  "新增元素": {"角色集": [], "事件集": [], "地点集": [], "线索集": [], "阵营集": [], "物品集": [], "其他事项集": []},
  "修改元素": {"角色集": [], "事件集": [], "地点集": [], "线索集": [], "阵营集": [], "物品集": [], "其他事项集": []},
  "冲突声明": [],
  "质量说明": ""
}
```

- **`冲突声明`**：当原文信息前后矛盾、角色身份模糊、或者你无法判断某个元素该归入新增还是修改时，把你的疑虑写进这个数组。每条是一个字符串，例如 `"第42章称张三是李四的兄弟，但第15章说是堂兄，存在矛盾"`。这些声明不会阻塞校验，但会保留在过程产物中供后续审计参考。
- **`质量说明`**：对本章 Delta 整体质量的自我评估，例如 `"本章主要是日常过渡，新增元素较少，信息增量集中在角色情感线"` 或 `"原文中出现了大量新角色但关系不明确，暂按路人处理"`。

### 新增元素的完整字段示例

> 下方为通用占位骨架。示例中 `标签集` 和 `详情` 用 `<...>` 占位符示意"这里该写什么类型的信息"——它们是元素的语义档案载体，应当根据本章原文承载丰富内容。占位符**不是必填的固定键**，键名/数量/具体语义都由你按本章实际内容和本作题材自由决定。**不知道的字段直接不写键**，不要为凑 schema 编造内容。
>
> **关于标签集**：优先按本章语义自由写短词；如果你明确知道某个受控前缀和值，也可以写成 `前缀:值`。不需要为了命中字典而猜测受控值，猜不准时直接写自由短词即可。生成后 `compress_tags.py` 会自动按 `tag_limits.json` 限额和受控前缀做归档下层，不需要你手工克制。
>
> **关于详情**：除示例中标注的必填项外，每个元素的详情应该有若干个 LLM 自定义的键，用来承载原文呈现的具体语义（对角色/物品是实力/性格/规则等；对事件是起因/经过/结果/冲突点/情绪点等）。键名按本章内容贴合命名，不要套抽象固定模板；键名只能是中英文数字组合（不能含标点/空格/下划线）。

**角色集** 元素（`详情` 必填：`提取理由`、`首次章节`、`最近章节`）：

```json
{
  "名称": "角色名",
  "是否主角": false,
  "性别": 2,
  "年龄": 0,
  "所属阵营": ["<已存在或本Delta新增的阵营名>"],
  "关系": ["<关系类型>:<已有或新增的角色名>"],
  "分组": "<本作题材下合理的分组名>",
  "别名": ["<原文出现的别名>"],
  "标签集": ["<自由短词>", "<另一个自由短词>", "<可选:前缀:值，自由发挥或参考字典>"],
  "介绍": " ",
  "详情": {
    "提取理由": "...",
    "首次章节": "{{ chapter_id }}",
    "最近章节": "{{ chapter_id }}",
    "<原文里这个角色的某项设定>": "...",
    "<另一项设定>": "...",
    "...": "..."
  }
}
```

角色字段说明：`是否主角`、`性别`（0男/1女/2未知）、`年龄`、`生日` 等机械字段**不需要你填写**，脚本会自动补默认值和类型转换。你只需关注 `名称`、`关系`、`所属阵营`、`分组`、`标签集`、`介绍`、`详情` 等语义内容。`关系` 是字符串数组，每项格式 `"关系类型:角色名"`，不写成对象。

**事件集** 元素（`详情` 必填：`提取理由`、`涉及章节`）：

```json
{
  "名称": "事件名",
  "发生地点": "<已存在或本Delta新增的地点名>",
  "参与成员": ["<参与角色1>", "<参与角色2>"],
  "重量级": 50,
  "目标事件": ["<可选:本事件推动的后续事件名>"],
  "分组": "<剧情段名，控制在20字内>",
  "标签集": ["<自由短词>", "<可选:前缀:值，自由发挥或参考字典>"],
  "别名": [],
  "介绍": "参与成员在事件中分别做了什么、推进了什么冲突。",
  "详情": {
    "提取理由": "...",
    "涉及章节": "{{ chapter_id }}",
    "<自由提炼的自定义键名，如：起因/背景>": "...",
    "<自由提炼的自定义键名，如：关键转折>": "...",
    "<自由发挥的任意键名，如：情绪爆发点>": "...",
    "...": "..."
  }
}
```

事件字段说明：`时间` 字段**不需要你填写**，脚本会根据 `详情.涉及章节` 自动推导。`发生地点` 是单个字符串（不是数组）；`目标事件` 是字符串数组（不是字符串）。`详情.涉及章节` 填写本章章节号即可，格式纠正由脚本完成。

事件 `分组` 用**简单的剧情段名控制在20个字以内**；如需更精细的语义编码，可使用复合格式 `段号-剧情段名+剧情线主题+叙事功能+爽点情绪点+冲突悬念类型`（例如 `"0010-退婚事件+情感尊严线+冲突爆发+羞辱反击+身份与尊严"`），但简单字符串完全合规，不强求。

**地点集** 元素（`详情` 必填：`提取理由`、`首次章节`、`最近章节`）：

```json
{
  "名称": "地点名",
  "父级地点": "<可选:上级地点名>",
  "分组": "<分组名>",
  "别名": [],
  "标签集": ["<自由短词>", "<另一个自由短词>"],
  "介绍": "地点是什么、在本章承担什么作用。",
  "详情": {
    "提取理由": "...",
    "首次章节": "{{ chapter_id }}",
    "最近章节": "{{ chapter_id }}",
    "<原文里关于这个地点的某项设定>": "...",
    "...": "..."
  }
}
```

**线索集** 元素（`详情` 必填：`提取理由`、`首次章节`、`最近章节`）：

```json
{
  "名称": "线索名",
  "涉及事件": ["<相关事件名>"],
  "分组": "<分组名>",
  "别名": [],
  "标签集": ["<自由短词>", "<另一个自由短词>"],
  "介绍": "线索内容、暗示了什么悬念、后续可能如何回收。",
  "详情": {
    "提取理由": "...",
    "首次章节": "{{ chapter_id }}",
    "最近章节": "{{ chapter_id }}",
    "<这条线索的某项语义>": "...",
    "...": "..."
  }
}
```

**阵营集** 元素（`详情` 必填：`提取理由`、`首次章节`、`最近章节`）：

```json
{
  "名称": "阵营名",
  "父级阵营": "<可选:上级阵营名>",
  "座落地点": "<阵营所在地点>",
  "分组": "<分组名>",
  "别名": [],
  "标签集": ["<自由短词>", "<另一个自由短词>"],
  "介绍": "阵营性质、核心人物、与其他阵营的关系。",
  "详情": {
    "提取理由": "...",
    "首次章节": "{{ chapter_id }}",
    "最近章节": "{{ chapter_id }}",
    "<原文里关于这个阵营的某项设定>": "...",
    "...": "..."
  }
}
```

**物品集** 元素（`详情` 必填：`提取理由`、`首次章节`、`最近章节`）：

```json
{
  "名称": "物品名",
  "分组": "<分组名>",
  "别名": [],
  "标签集": ["<自由短词>", "<另一个自由短词>"],
  "介绍": "物品来历、能力、在本章的作用。",
  "详情": {
    "提取理由": "...",
    "首次章节": "{{ chapter_id }}",
    "最近章节": "{{ chapter_id }}",
    "<原文里关于这个物品的某项设定>": "...",
    "...": "..."
  }
}
```

**其他事项集** 元素（`详情` 必填：`提取理由`；用于设定/世界规则/创作备注；**不要**写章节追溯字段）：

```json
{
  "名称": "事项名",
  "分组": "<分组名>",
  "别名": [],
  "标签集": ["<自由短词>", "<可选:前缀:值，自由发挥或参考字典>"],
  "介绍": "事项内容、对故事世界观或叙事机制的影响。",
  "详情": {
    "提取理由": "...",
    "<这条设定的某项语义>": "...",
    "...": "..."
  }
}
```

### 修改元素

`修改元素` 是 patch，不是完整元素重写：只填写本章**新增或变化**的字段，不要为了凑 schema 回填旧字段、不要重写旧 `介绍`。建议在 `详情` 里写 `提取理由`；角色/地点/线索/阵营/物品若能确认本章再次出现，可写 `最近章节: "{{ chapter_id }}"`，但不要强行回填 `首次章节`。事件若能确认涉及本章，可在 `详情.涉及章节` 中补充本章；不确定时把证据写入普通详情键，后续 normalize/治理会收敛。

### 元素质量要求与前置治理原则

提取和整合故事元素时，除了满足 JSON 结构要求，更要关注**文学内容的质量**。这些规则来自故事结构质量治理要求，需要在 Delta 提取阶段就予以贯彻：

1. **信息密度与质量**：
   - **标签丰富度**：多使用有信息密度的标签；不要局限于受控字典，自由短词同样欢迎。
   - **介绍详尽度**：事件的 `介绍` 必须明确描述参与成员分别做了什么；不能只写一个空泛的标题。
   - **详情价值**：充分利用 `详情` 字段记录非标准但有文学价值的信息。
2. **逻辑与引用**：
   - 剧情和设定必须符合原文逻辑。
   - 交叉引用（如阵营、地点、参与成员）必须自洽。
   - **未确认信息不得写成确定事实**，请写进 `冲突声明` 或 `详情.疑似设定` 中。
3. **辨识度与去重**：
   - 命名要有辨识度，不要把“那个男人”、“老者”等普通称呼当做主名称。
   - 确保本章出现的人物如果在前文有别名，要归入原有元素的修改中，不要当成新人物。
4. **分组与叙事归类**：
   - 分组必须有真正的叙事归类价值，绝不使用空泛的 `其他`、`杂项`、`默认`、`未知`。
   - **事件分组不分层，按行文顺序排列**，优先表达简短剧情段名（控制在20字内）。
   - 若能明确五个语义维度，可使用复合分组格式：`段号-剧情段名+剧情线主题+叙事功能+爽点情绪点+冲突悬念类型`（如 `0010-退婚事件+情感尊严线+冲突爆发+羞辱反击+身份与尊严`）。没有把握时，简单字符串完全合规。
5. **提取门槛与数量**：
   - 数量仅作为丰富度参考，绝不作为拆书提取的硬门槛。**如果原文信息不足，绝对不得为了达标而编造或新增无用元素。**


### 脚本自动处理的字段（你不需要填写）

以下字段会由 `coerce_delta.py` 和 `normalize` 脚本自动补齐或修正，**你可以完全忽略它们**：

- 角色的 `是否主角`、`性别`、`年龄`、`生日` — 缺失时自动补默认值，类型错误自动转换
- 事件的 `时间` — 根据 `详情.涉及章节` 自动推导，不要手动填写
- 章节号格式（四位补零、全角逗号分隔）— 脚本自动归一化
- 顶层追溯字段位置（`提取理由`/`首次章节`/`最近章节`/`涉及章节` 误写到元素顶层）— 脚本自动迁回 `详情`

### 常错点速查

- `是否主角`：bool（`true`/`false`），不是 `0`/`1`。
- `性别`：int `0`/`1`/`2`，不是 `"男"`/`"女"`。
- `首次章节`/`最近章节`/`涉及章节`：如果填写，必须放在 `详情` 里，不能放在元素顶层；修改元素不要强行回填旧的 `首次章节`。
- `提取理由`：建议放在 `详情` 里，便于追溯；过程校验不会因为缺少它反复卡住，但质量治理会检查证据是否充分。
- `发生地点`：字符串，不是数组。优先填写已注册的正式地点或父级地点（如 `同福客栈`）；`门口`、`女寝`、`客房` 等细粒度子空间若没有持续叙事价值，写入事件 `详情.具体地点`、`详情.场景位置` 或 `介绍`，不要为了通过校验机械注册为地点。
- `目标事件`：数组，不是字符串。
- `关系`：`["类型:名称", ...]` 字符串数组，不是对象。
- 事件 `分组`：优先写20字内剧情段名（如 `退婚冲突`）；若用复合格式，必须是 `四位段号-剧情段名+剧情线主题+叙事功能+爽点情绪点+冲突悬念类型`。不要跨章重排旧事件分组。
- 引用一致性：`所属阵营`/`参与成员`/`父级地点`/`座落地点`/`涉及事件`/`目标事件`/`关系` 尽量使用**当前结构索引已有元素**或**本 Delta 新增元素**的正式名称；过程阶段前向引用会先作为 warning，周期/最终治理再收敛。`发生地点` 可在过程/治理阶段保留未注册的细粒度子空间 warning，但最终交付前应收敛为已注册正式地点；陌生且不确定的名称优先放进详情说明，不要为了通过校验编造元素。
- 不要为达数量编造原文不存在的元素。

## 其他要求

- 这是逐切片任务；本 Delta 只能对应第{{ "%03d" | format(chapter_seq) }}个拆分单元，禁止合并多个章节/编号/小节。
- Delta 是可合并 patch：新增元素尽量补齐标准字段，但不要为凑字段编造原文没有的信息；缺失的默认字段会由 normalize 阶段补齐。
- 不要编写 Python 或脚本来生成 Delta。直接写 JSON 内容即可；JSON 语法、常见类型和顶层追溯字段位置会由 `repair_llm_json.py` / `coerce_delta.py` 机械处理。只有名称、分类、引用语义、原文证据这类内容问题需要你判断修复。
- 上述未覆盖的边角情况以 `scripts/validate_delta.py` 校验报告为准；过程阶段 warning 不需要反复重写 Delta，只有 error 才需要修。

## 本章原文

```markdown
{{ chapter_text }}
```

 "请严格输出指定 JSON，不要输出 Markdown、代码块、或前后缀文本。"
