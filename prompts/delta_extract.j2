# 第{{ "%03d" | format(chapter_seq) }}章Delta提取任务

## 唯一产出

本章Delta JSON：`{{ delta_path }}`

本任务只做结构提取。**不得修改章节分析MD**；必须基于下方已完成的章节分析、原文和当前故事结构索引输出本章Delta，不要输出完整故事结构JSON。

## 当前故事结构索引

```text
{{ summary }}
```

## 已完成章节分析

```markdown
{{ analysis_text }}
```

## Delta JSON硬格式

```json
{
  "章节": "第{{ "%03d" | format(chapter_seq) }}章",
  "新增元素": {"角色集": [], "事件集": [], "地点集": [], "线索集": [], "阵营集": [], "物品集": [], "其他事项集": []},
  "修改元素": {"角色集": [], "事件集": [], "地点集": [], "线索集": [], "阵营集": [], "物品集": [], "其他事项集": []}
}
```

要求：
- 这是逐切片任务；本Delta只能对应第{{ "%03d" | format(chapter_seq) }}个拆分单元，禁止合并多个章节/编号/小节。
- 新增元素必须补齐所有标准字段；非标准信息必须放入详情。
- 每个新增或修改元素的详情必须填写非空 `提取理由`。
- 角色、地点、线索、阵营、物品在详情内提供四位字符串 `首次章节`、`最近章节`，例如 `"{{ chapter_id }}"`。
- 事件详情提供升序 `涉及章节`，并在顶层提供时间；本章新事件时间固定为 `{{ chapter_time }}`。
- 详情字段、引用、标签和治理字段必须遵守 `references/delta_extraction.md`；`schemas/delta.schema.json` 只是任务包中的轻量格式提示，入库前的强校验以 `scripts/validate_delta.py`（详情字段规则见 `scripts/story_schema_rules.py`）为准。不能为了满足数量要求编造原文没有的信息。

## 本章原文

```markdown
{{ chapter_text }}
```
