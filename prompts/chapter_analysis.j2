# 第{{ "%03d" | format(chapter_seq) }}章章节分析任务

## 唯一产出

章节分析MD：`{{ analysis_path }}`

本任务只做文学理解和章节分析。**不得创建或修改 Delta JSON**；完成MD后，重新运行 `run_pipeline.py run`，系统会生成独立的 Delta 提取任务。

## 章节分析MD模板

章节分析器先输出 MD，不输出 JSON。以下 10 个部分必须完整保留且按顺序输出；原文没有明确内容时，写“本章无明确新增”并说明依据，不能删除章节。

```markdown
# 第{{ "%03d" | format(chapter_seq) }}章 {标题}

## 1. 剧情梗概
约500字，概括本章主要事件、人物行为、情节转折。

## 2. 出场人物
- 人物A：本章行为、情绪状态、关系变化、是否首次出现。

## 3. 核心冲突
冲突双方、冲突目标、冲突推进方式。

## 4. 信息增量
新增世界观、人物背景、地点、阵营、规则、线索或设定。

## 5. 伏笔与悬念
伏笔、悬念、后续可能回收点、证据原文。

## 6. 爽点 / 虐点 / 情绪点
压抑、期待、羞辱、反击、打脸、逆袭、热血、悬念、感动、虐心、危机感、成就感等。

## 7. 章节功能判断
开篇铺垫/冲突升级/人物塑造/世界观展开/高潮推进/转折/收束/过渡等。

## 8. 事件分组与标签建议
只为**本章**规划连续剧情段；不要跨章节承接分组，也不要为每个事件各起一个分组。`事件.分组`表示同一目标/冲突链在本章内的起因、升级、转折、收束；地点、人物、情绪强度变化本身不是新分组理由。仅当叙事主轴明确切换时才新建分组。

分组短名控制在40字以内，压缩包含剧情、剧情线主题、叙事功能、爽点/情绪点、冲突悬念类型；不要写段号，脚本合并后会按全书事件顺序统一分配8位段号。

每个事件还要给出一个四维复合标签建议，格式固定为 `剧情线主题+叙事功能+爽点/情绪点+冲突悬念类型`，例如 `侠之真义+主题点化+感动+观念冲突`。该标签不写段号、不写剧情段名、不带冒号，后续 Delta 应直接放入事件 `标签集`，并保留在标签集供界面直读。

### 8.1 本章事件脊线
| 事件号 | 事件锚点 | 承接关系 |
|---|---|---|
| E01 | 本章第一个原子事件 | 新起/升级/转折/收束 |

### 8.2 本章连续剧情段
| 剧情段短名 | 覆盖事件 | 起止判断 |
|---|---|---|
| 退婚尊严线冲突爆发羞辱反击身份尊严 | E01-E03 | 说明为何属于同段，以及在哪个事件发生主轴切换 |

## 9. 画面 / 分镜 / 视觉资产候选
| 候选编号 | 对应事件 | 画面价值 | 画面类型 | 核心画面 | 出场人物 | 地点 | 关键物品 | 情绪氛围 | 镜头建议 | 是否进入视觉资产 |
|----------|----------|----------|----------|----------|----------|------|----------|----------|----------|------------------|

没有合格候选时，保留表头并增加一行“无高价值画面候选”。

## 10. 结构提取提示
列出最应该进入故事结构JSON的角色、事件、地点、线索、阵营、物品、其他事项。
```

## 本章原文

```markdown
{{ chapter_text }}
```
