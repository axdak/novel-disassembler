# 周期结构审计任务包 {{ "%03d" | format(start) }}-{{ "%03d" | format(end) }}

## 产出要求

请基于本任务包审计第{{ "%03d" | format(start) }}-{{ "%03d" | format(end) }}章的结构累积质量，输出治理补丁：

- 补丁路径：`{{ correction_path }}`
- 补丁必须使用 Delta 格式，只写 `新增元素`、`修改元素` 和可选 `治理操作`。
- 不要直接重写完整故事结构 JSON。
- 若本周期内确实无需治理：必须显式声明 `治理类型: "no_change"`，并附**非空**的 `质量说明`（写明已检查角色/事件/地点/线索/阵营/物品的结论）和 `证据范围`（数组，列出本周期已审计章节，例如 `["第001章", "第002章", ...]`）。严禁提交 `新增元素`、`修改元素`、`治理操作` 全空且无 no_change 元数据的补丁，校验器会直接拦截。

主控Agent必须直接读取本任务包、写出真实治理补丁，再运行：

```bash
python {{ run_pipeline_path }} commit-governance {{ project_dir }} {{ correction_path }}
```

## 审计重点

- 合并跨章节重复角色、地点、线索、阵营、物品。
- 将路人、临时称谓、误识别对象降级或合并到详情。
- 整理伏笔、关系和事件引用，避免名称漂移。
- 标签治理时，仅保留有跨元素检索、筛选、聚合或导航价值的标签；纯描述、一次性情境或与介绍重复的标签可放入 `治理操作.治理标签`。无法明确判断时保留。
- `治理标签` 每项必须给出 类型、名称、保留标签、降级标签、理由；保留与降级必须完整覆盖该元素当前标签集。降级项会自动写入可逆的 `详情.补充标签`。
- 事件数组顺序代表章节逻辑顺序：治理新增的历史聚合事件必须按最小涉及章节插入；修改既有事件不得改变其时间、涉及章节或数组位置。
- 事件分组推荐使用 段号-剧情段名+剧情线主题+叙事功能+爽点情绪点+冲突悬念类型，例如0010-退婚事件+情感尊严线+冲突爆发+羞辱反击+身份与尊严；段号后必须写剧情结构信息，严禁0010-0001这类编号套编号。治理后按事件数组顺序全量重排段号，并拆分非连续重复剧情段。
- 只修正结构质量问题，不凭空补充原文没有的信息。

{% if full_story_json -%}
## 完整故事结构

```json
{{ full_story_json }}
```
{%- elif story_index -%}
## 当前故事结构索引

```text
{{ story_index }}
```
{%- if relevant_details %}

## 本周期相关老元素详情

```json
{{ relevant_details }}
```
{%- endif %}
{%- if previous_correction %}

## 上一个审计周期治理结果 ({{ previous_correction.name }})

```json
{{ previous_correction.content }}
```
{%- endif %}
{%- endif %}
{% for ch in chapters %}
## 第{{ "%03d" | format(ch.seq) }}章材料

{% if ch.missing -%}
[缺失] 原文拆解文件：第{{ "%03d" | format(ch.seq) }}章
{%- else -%}
- 原文：`{{ ch.chapter_path }}`
- 章节分析：`{{ ch.analysis_path }}`
- Delta：`{{ ch.delta_path }}`

### 章节分析

```markdown
{{ ch.analysis_excerpt }}
```

### Delta

```json
{{ ch.delta_excerpt }}
```
{%- endif %}
{% endfor %}
